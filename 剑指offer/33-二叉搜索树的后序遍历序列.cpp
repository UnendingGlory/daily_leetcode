#include "header.h"


// 判断数组是不是二叉搜索树的后序遍历结果
// 二叉搜索树：左子树小于根节点，右子树大于根节点

// 递归判断
class Solution {
public:
    bool verifyPostorder(vector<int>& postorder) {
      int n = postorder.size();
      if (n == 0) return false;

    }
};
