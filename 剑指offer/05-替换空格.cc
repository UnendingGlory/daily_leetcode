#include "header.h"

class Solution {
public:
    string replaceSpace(string s) {
        string ans;
        for (int i = 0; i < s.size(); ++i) {
             
        }
    }
};

class Solution:
    def replaceSpace(self, s: str) -> str:
        return s.replace(" ", "%20")